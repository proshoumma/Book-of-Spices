import {
  primaryFontSize,
  themeColor,
  themeFont,
  margin,
} from '../../../../globalStyles'

export default {
  container: {
    marginBottom: margin,
    justifyContent: 'center',
    alignItems: 'center'
  },

  label: {
    fontFamily: themeFont,
    color: themeColor,
    fontSize: primaryFontSize + 6
  }
}
