import { margin } from '../../globalStyles'

export default {
  container: {
    flex: 1,
    paddingTop: 60,
    paddingBottom: 20,
    paddingLeft: margin,
    paddingRight: margin,
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between'
  }
}